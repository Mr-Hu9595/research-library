---
name: research-library
description: 调研成果库系统。当项目需要"调研外部信息（GitHub 类似项目 / 行业产品 / 官方文档 / 竞品）→ 沉淀成果 → 反哺决策与开发"时，标准化调研动作 + 统一成果索引 + 决策状态/落地追踪闭环，让每条调研可查、可复用、不重复。通用（不限领域、不限是否 PM-AI 协作）。触发词：调研、研究、调研成果、调研索引、调研库、对标、竞品调研、GitHub 调研、research。
---

# 调研成果库系统（Research Library）

项目做调研（对标、竞品、技术选型、GitHub 同类项目）最大的浪费不是"没调研"，而是**调研成果散落在对话和文档里、重复调研、结论没人消费**。本 Skill 提供一套经过实战打磨的调研资产化流程，让每次调研都变成项目可复用的资产。

## 这套系统解决什么问题

| 痛点 | 本系统的解法 |
|---|---|
| 调研做了很多，成果散落各处，找不到 | 统一索引（5 要素），一次入库终身可查 |
| 同一个主题反复调研 | **先查后研**：调研前查索引，已有结论直接复用 |
| 调研结论没人落地，白做 | **可落地清单 + 落地引用**：每条结论挂任务编号，落地状态可查 |
| 调研报告堆发现不堆结论，看完不知道怎么办 | **结论优先**：明确推荐 + 理由 + 下一步清单 |
| 调研只抄项目名，不提炼可复用的东西 | **模式提炼**：找"架构/功能类似"项目，提炼 模式×场景×优缺点 |

## 核心原则（调研资产化五条）

1. **先查后研**：调研前查索引，防重复、可复用已有结论（比调研便宜）
2. **结论优先**：推荐 + 可落地清单给到位；"发现"从简、"怎么做"给准
3. **模式提炼 > 项目罗列**：找"架构/功能类似"的项目（**不要求用途一致**），提炼可复用模式，不抄名
4. **沉淀入库**：标准报告 + 5 要素索引回填，成果不留在对话里
5. **闭环可查**：决策状态（拍板/待决策/参考）+ 落地引用（被谁消费）+ 未消费清单，一眼知道"哪些落地了、哪些没有"

## 目录结构（在项目中初始化）

```
<项目根>/
├── docs/
│   └── research/
│       ├── INDEX.md                  # 调研索引（5 要素，SSOT 入口）
│       ├── <YYYY-MM-DD>-<主题>.md    # 调研报告（每份一份，标准结构）
│       └── README.md                 # 本库说明（三库分类 + 使用方式）
```

## 标准化调研流程（五步）

```
Step 0 先查后研    → 查 INDEX.md：有没有已有结论可复用 / 会不会重复
                    （有 → 直接引用 + 更新落地引用；无 → 进入 Step 1）
Step 1 定目标范围  → 调研问题 + 范围（结合项目实际：找"架构/功能类似"项目，
                    不要求用途一致——用途是表，架构是里）
Step 2 多源执行    → GitHub 多关键词广撒网（英文关键词优先，中文常空返回）
                    + Web / 官方文档 / 行业产品 / 竞品
                    + 取代表性样本（10-20 个），逐个读架构文档/README
Step 3 提炼结论    → 提炼"模式 × 适用场景 × 优缺点"（不抄名、不堆链接）
                    + 明确推荐 + 理由 + 可落地清单 + 决策状态
Step 4 沉淀入库    → 写标准报告 → 回填 INDEX.md（5 要素）→ 三库分类
```

**调研方法论要点**（实战验证）：
- 关键词：英文为主、多组合广撒网（单一关键词漏检率高）
- 样本：取 10-20 个代表性仓库/产品，覆盖多种模式（A 同类/B 同构/C 重型/D 协作/E SaaS），不是越多越好
- 提炼：每项 = 架构简介 + 知识库/核心机制处理模式；跨项对比出模式清单
- 只读证明：调研不改代码/不写库/不调 LLM；来源（GitHub 仓库/URL/文档）可复现
- 诚实：无法核实的如实标注"待核实"，不猜；并按"无法核实处置框架"分级定处置（见下）

## 无法核实项的处置框架（三问定级 + 统一动作）

调研/核对中总有无法核实的东西。**无法核实 ≠ 放弃**——按框架分级处置，让每项都有归属、可解冻。

**第一问：能不能解除？**
| 情形 | 处置 |
|---|---|
| 能解除 + 成本低 | 排期补能力（如格式转换/补抓数据后即可核实） |
| 能解除 + 成本高/收益低 | 如实标注"需投入 X 才能核实，收益低，搁置"，记入未消费清单 |
| 无法解除（外部限制：闭源/反爬/已下架/时效性） | 定性"永久性限制"，记录原因，不再纠结 |

**第二问：有没有替代口径？**
- 换一个**可核实的近似指标**（如 .doc 图片数 → 用页数/章节结构近似）
- 或用**保守估计 + 明确标注**（如"估算非承诺"），如实

**第三问：影响面多大？**
- 影响**决策** → 必须补核到能核实才拍板
- 影响**小 / 不阻塞** → 标注后继续，等有条件再补

**统一动作（每项必做）**：
1. 写进报告「无法核实项」节
2. **给"解除路径"**：每项附一句"补什么就能核实"——让无法核实项可被后续解冻
3. 回填调研库 INDEX 未消费清单（标"待补能力核实"），形成闭环

## 索引 5 要素 + 三库分类 + 决策状态

**INDEX.md 一行一条调研（5 要素）**：
```
| 主题 | 日期 | 报告路径 | 核心结论(1-3句) | 决策状态 | 落地引用 | 未消费清单 |
```

**决策状态**：
- ✅ 已拍板（结论已被采用，后续不再调研，直接引用）
- ⏳ 待决策（结论给了，等拍板）
- 📌 仅参考（知识资产，按需检索）

**三库分类（README 说明）**：
| 库 | 内容 | 消费者 |
|---|---|---|
| 决策库 | ✅ 已拍板结论 | 后续直接引用，不再调研 |
| 待落地清单 | ⏳ 未消费的可执行建议 | 排队派单 |
| 知识资产库 | 📌 长期参考（结构/模式/清单） | 按需检索 |

## 闭环机制

1. **调研后回填索引**（Step 4 必做）——索引滞后 = 知识丢失第一步
2. **落地后更新状态**：某条结论被任务消费 → 在「落地引用」列挂任务编号；任务完成 → 标记 ✅
3. **新调研前先查索引**（Step 0 必做）——防重复，这是"发挥最大效用"的关键
4. **结论被消费但没落地的**：留在「未消费清单」，后续排队，不丢

## 模板

- 调研报告模板：`references/templates/research-report-template.md`
- 调研索引模板：`references/templates/research-index-template.md`

## 适用场景

- 技术选型（数据库/框架/工具）调研
- 竞品 / 对标产品调研
- GitHub 同类项目模式提炼
- 行业标准 / 规范调研
- 任何"查外部信息 → 要产出结论 → 结论要能被后续消费"的场景

**不限于投标/AI 领域、不限于 PM-AI 协作模式**——单人开发、传统团队、咨询项目都能用。
